# Container name
CONTAINER_NAME=mcpo-ui

# Ports
UI_PORT=80
MCPO_PORT=8000

# Base URL for accessing MCPO from browser (leave empty to use http://localhost:MCPO_PORT)
# Examples: https://mcpo.example.com, http://192.168.1.100:8000
MCPO_BASE_URL=

# API Key for MCPO authentication (required)
MCPO_API_KEY=your-secret-key-here

# UI Authentication (optional - uses HTTP Basic Auth via Caddy)
# Leave both empty to disable authentication
# To generate and escape password hash for .env file:
#   docker run --rm caddy caddy hash-password --plaintext yourpassword | sed 's/\$/\$\$/g'
UI_USERNAME=
UI_PASSWORD_HASH=
